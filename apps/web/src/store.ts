import { configureStore, createSlice, PayloadAction } from '@reduxjs/toolkit';
type AuthUser={id:string;email:string;role:string;fullName?:string;phone?:string}; type AuthState={accessToken:string|null;refreshToken:string|null;user:AuthUser|null}; const initialState:AuthState={accessToken:null,refreshToken:null,user:null}; const auth=createSlice({name:'auth',initialState,reducers:{setSession:(_,action:PayloadAction<AuthState>)=>action.payload,clearSession:()=>initialState}}); export const {setSession,clearSession}=auth.actions; export const store=configureStore({reducer:{auth:auth.reducer}}); export type RootState=ReturnType<typeof store.getState>;
